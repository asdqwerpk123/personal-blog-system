# personal-blog-system 接口文档

## 在线文档地址
https://g8xgnpu60o.apifox.cn

## 已完成内容
1. 发起快捷请求
2. 手动新建接口
3. 文件导入
4. 生成在线文档

## 接口列表
- GET /admin/user/{id} 根据ID查询用户
- GET /admin/role/{id} 根据ID查询角色
- GET /admin/article/{id} 根据ID查询文章
- GET /admin/category/{id} 根据ID查询分类
